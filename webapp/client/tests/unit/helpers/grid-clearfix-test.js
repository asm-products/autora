import { gridClearfix } from '../../../helpers/grid-clearfix';
import { module, test } from 'qunit';

module('Unit | Helper | grid clearfix');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gridClearfix(42);
  assert.ok(result);
});
