import { clipText } from '../../../helpers/clip-text';
import { module, test } from 'qunit';

module('Unit | Helper | clip text');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = clipText(42);
  assert.ok(result);
});
