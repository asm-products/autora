{{#if user.photo}}
    <img id="ember745" src="{{imageUrl}}" class="circular profile">
{{else}}
    {{gravatar-image email=user.email defaultImage=placeholderUrl size=100 class="circular profile"}}
{{/if}}
